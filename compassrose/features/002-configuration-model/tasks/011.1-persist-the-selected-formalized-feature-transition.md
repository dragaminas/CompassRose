# Task 011.1: Persist the selected formalized feature transition

## Task ID
`F002-T11-C1`

## Parent Task
`F002-T11`

## Parent Feature
`002-configuration-model`

## Goal
Add only the missing main([]) checkpoint that changes a selected formalized feature to task_planning_pending after successful preflight and feature selection, with a focused executable regression test.

## First Executable Step
Edit tests/main.test.ts to add a failing main([]) fixture asserting that a selected formalized feature transitions its state.md lifecycle to task_planning_pending while preserving the remaining state content.

## Minimum Progress Evidence
- tests/main.test.ts contains an executable transition fixture that asserts main([]) returns 0, state.md now contains task_planning_pending, non-lifecycle content is unchanged, and the output identifies the feature and transition.
- src/cli/main.ts persists only the formalized to task_planning_pending transition after existing preflight and deterministic feature selection succeed.
- npx vitest run tests/main.test.ts -t "formalized feature transitions to task_planning_pending" executes the focused fixture successfully.
- npx vitest run tests/main.test.ts passes after the focused transition is implemented.

## Review Findings
- src/cli/main.ts:179-186 reports a selected formalized feature and returns without persisting task_planning_pending.
- tests/main.test.ts:471-513 has no executable test for the required transition or preservation of state.md content.
- The targeted quality gate passed with all tests skipped because the required transition test was absent.
- The implementer context was sufficient and explicitly required this test-first change.

## Scope
Allowed:
- `src/cli/main.ts`
- `tests/main.test.ts`

Forbidden:
- `docs/`
- `src/contracts/`
- `src/config/`
- `src/doctor/`
- `proto/`
- `package.json`
- `tests/other files`

## Constraints
- Use test_guided development: add the failing regression test before implementation changes.
- Preserve existing preflight ordering, deterministic numeric feature selection, request_pending derivation, malformed-lifecycle diagnostics, completed-feature handling, and doctor behavior.
- For a selected formalized feature, update only the lifecycle value in state.md and preserve all remaining state content.
- Emit a deterministic transition message and stop at task_planning_pending; do not invoke external tools or generate a task artifact.
- Do not modify configuration, contracts, feature documentation, project state, or unrelated lifecycle transitions.

## Acceptance Criteria
- With valid configuration and a selected formalized feature, main([]) returns 0 and persists task_planning_pending in that feature's state.md.
- The persisted state preserves all non-lifecycle content and the output deterministically identifies the feature and both lifecycle states.
- Existing preflight failure behavior and feature-selection regressions remain unchanged and passing.
- The focused transition test and the full tests/main.test.ts suite execute successfully.

## Quality Gates to Run
```bash
npx vitest run tests/main.test.ts -t "formalized feature transitions to task_planning_pending"
npx vitest run tests/main.test.ts
npm run typecheck
npm test
git diff --check
```
