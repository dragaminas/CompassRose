# Task 009.1: Complete tracked-worktree test coverage for CLI preflight

## Task ID
`F002-T09-C1`

## Parent Task
`F002-T09`

## Parent Feature
`002-configuration-model`

## Goal
Complete the missing test-guided evidence for F002-T09 by exercising a modified tracked file in a real temporary Git repository and verifying that main([]) rejects it under the canonical clean-worktree policy, while retaining untracked rejection and explicit allow-dirty success coverage.

## First Executable Step
In tests/main.test.ts, add a failing test in the existing dirty-worktree suite that uses createTempGitWorkspace, modifies a file committed by the fixture, invokes main([]), and asserts exit code 1 with runtime preflight and git_policy diagnostics.

## Minimum Progress Evidence
- tests/main.test.ts contains a real temporary Git fixture with a committed file that is modified before main([]) runs.
- The tracked-change test asserts exit code 1 and diagnostics containing runtime preflight and git_policy.
- The existing untracked rejection and explicit allow-dirty success tests remain present and pass.
- Fixture setup failures fail the test instead of returning without assertions.

## Review Findings
- The original diff covers only untracked changes, despite the task constraint requiring tracked and untracked worktree coverage.
- The new tests can silently pass when file creation or Git setup fails because errors are swallowed or cause an assertion-free return.

## Scope
Allowed:
- `tests/main.test.ts`

Forbidden:
- `src/cli/main.ts`
- `docs/compassrose/CONFIG.md`
- `docs/compassrose/PROJECT_STATE.md`
- `docs/features/002-configuration-model/`
- `src/config/`
- `src/doctor/`
- `src/contracts/`
- `proto/`
- `global external-tool configuration files`

## Constraints
- Keep the existing src/cli/main.ts worktree-check implementation unchanged.
- Use the existing git status behavior and real temporary Git repository mechanism.
- Preserve the untracked dirty rejection and explicit allow-dirty success cases.
- Do not modify documentation, contracts, provider configuration, or global tool settings.

## Acceptance Criteria
- A real temporary Git repository commits a fixture file, then modifies that tracked file before main([]) runs.
- The tracked dirty-worktree test exits with code 1 and emits runtime preflight and git_policy diagnostics.
- Untracked dirty-worktree rejection and explicitly allowed dirty-worktree success remain covered and passing.
- The test fixture cannot silently pass without assertions when Git or file setup fails.

## Quality Gates to Run
```bash
npx vitest run tests/main.test.ts
npm run typecheck
npm test
git diff --check
```
