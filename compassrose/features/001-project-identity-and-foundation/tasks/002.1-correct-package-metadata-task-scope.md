# Correction Task 002.1: Correct Package Metadata Task Scope

## Task ID
`F001-T02.1`

## Parent Task
`F001-T02`

## Feature ID
`001-project-identity-and-foundation`

## Title
`Correct Package Metadata Task Scope`

## Objective
Update task 002 and the feature state so their contracts match the real implementation path, including a minimal test baseline when needed to satisfy the configured quality gates.

## Review Findings
- High: The task doc is under-scoped relative to the repo's required gates. `docs/compassrose/CONFIG.md` requires typecheck and tests, but the task scope only allows package.json changes and the quality gates only run a one-off metadata check plus `git diff --check`. That leaves no explicit place for the regression test that was needed to close the gap.
- Medium: Files Likely Affected is stale because it lists only `package.json`, but the actual completion path also touches `tests/package-metadata.test.js`.
- Residual risk: The reviewer could not run `npm test` because `node` and `npm` were not on PATH in the review shell.

## Scope
Allowed:
- `docs/features/001-project-identity-and-foundation/tasks/002-package-metadata-and-entrypoint.md`
- `docs/features/001-project-identity-and-foundation/state.md`

Forbidden:
- `package.json`
- `src/`
- `tsconfig.json`
- `tests/`
- `docs/compassrose/CONFIG.md`
- `docs/features/001-project-identity-and-foundation/tasks/001-project-state-document.md`
- `docs/features/001-project-identity-and-foundation/tasks/003-typescript-foundation-config.md`
- `docs/features/001-project-identity-and-foundation/tasks/004-foundation-docs-alignment.md`

## Constraints
- Update only task 002's contract text and `docs/features/001-project-identity-and-foundation/state.md`.
- Do not change production code.
- Do not change unrelated tasks or unrelated documentation.
- Keep the correction narrowly focused on the missing test baseline allowance and gate coverage.

## Expected Changes
- Expand task 002 scope to allow a minimal test file when needed to satisfy configured quality gates.
- Add `tests/package-metadata.test.js` to task 002's `Files Likely Affected`.
- Replace the one-off metadata check with quality gates for `npm test`, `npm run typecheck`, `npm run build`, and `git diff --check`.
- Add a short implementation note explaining that the initial test baseline was required because Vitest fails when no test files exist.
- Update `state.md` to reflect the recovery bundle status, quality gates, and reviewer-approval next action.

## Acceptance Criteria
- Task 002 explicitly allows a minimal test file when needed to satisfy configured quality gates.
- `Files Likely Affected` in task 002 includes `tests/package-metadata.test.js`.
- `Quality Gates to Run` in task 002 includes `npm test`, `npm run typecheck`, `npm run build`, and `git diff --check`.
- Task 002 includes a short implementation note that the initial test baseline was required because Vitest fails when no test files exist.
- Task 002 is re-scoped so the future implementation can satisfy the repository's required gates without violating the task contract.
- `state.md` reflects the recovery bundle status, quality gates, and reviewer-approval next action.
- No production code or unrelated docs are changed.

## Files Likely Affected
- `docs/features/001-project-identity-and-foundation/tasks/002-package-metadata-and-entrypoint.md`
- `docs/features/001-project-identity-and-foundation/state.md`

## Quality Gates to Run
```bash
rg -n "tests/package-metadata\\.test\\.js|npm test|npm run typecheck|npm run build|Vitest fails when no test files exist|allow a minimal test file" docs/features/001-project-identity-and-foundation/tasks/002-package-metadata-and-entrypoint.md
git diff --check
```

## Implementation Note
The corrected task must treat the first test file as a required smoke-test baseline for the repo's configured quality gates, not as unrelated feature work.

## Completion Criteria
- Task 002 is re-scoped so the future implementation can satisfy the repository's required gates without violating the task contract.
- `state.md` reflects the current recovery bundle and remains within this correction task's scope.
