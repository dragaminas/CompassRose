# Request: VS Code Integration

I want a future VS Code integration for CompassRose.

The extension should act as a visualization layer over project-local documentation and state.

It may provide:

- roadmap explorer
- feature explorer
- task dashboard
- review dashboard
- state indicators

The documentation must remain the source of truth.
VS Code should not replace the CLI or the documentation model.

This feature is future scope, not MVP.
