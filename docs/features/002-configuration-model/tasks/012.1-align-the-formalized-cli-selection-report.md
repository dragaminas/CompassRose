# Task 012.1: Align the formalized CLI selection report

## Task ID
`F002-T12-C1`

## Parent Task
`F002-T12`

## Parent Feature
`002-configuration-model`

## Goal
Update only the existing formalized transition path and its focused test so the lifecycle value reported after the state write is task_planning_pending.

## First Executable Step
In tests/main.test.ts, add an exact assertion for CompassRose: selecting feature 002-formalized (lifecycle state: task_planning_pending) to the existing formalized transition test, then run the focused gate to expose the stale current report before changing src/cli/main.ts.

## Minimum Progress Evidence
- tests/main.test.ts contains the exact selection-message assertion.
- src/cli/main.ts updates the lifecycle value used by the selection report only after writeFileSync(statePath, updatedContent, 'utf8') succeeds.
- npx vitest run tests/main.test.ts -t "formalized feature transitions to task_planning_pending" passes.

## Review Findings
- src/cli/main.ts:179-197 leaves selectedLifecycle as formalized after persisting task_planning_pending, so the selection handoff is stale.
- tests/main.test.ts:568-570 lacks an exact selection-message assertion, and the implementation artifact reports already_complete despite the empty diff and unmet behavior.

## Scope
Allowed:
- `src/cli/main.ts`
- `tests/main.test.ts`

Forbidden:
- `src/config/**`
- `src/doctor/**`
- `src/contracts/**`
- `docs/**`
- `tests/configReader.test.ts`
- `tests/doctorCommand.test.ts`
- `tests/projectState.test.ts`
- `tests/testUtils.ts`

## Constraints
- Use test_guided development.
- Update the reported lifecycle only after the formalized-to-task_planning_pending persistence succeeds.
- Preserve the existing exact state.md content-preservation assertion and transition message.
- Preserve preflight ordering, malformed-lifecycle handling, feature ordering, and completed-feature behavior.
- Do not invoke external tools, generate a task artifact, or implement planner execution.
- Modify only src/cli/main.ts and tests/main.test.ts.

## Acceptance Criteria
- The formalized transition test contains the exact selection assertion CompassRose: selecting feature 002-formalized (lifecycle state: task_planning_pending).
- main([]) returns 0 and persists task_planning_pending in state.md.
- The complete state.md content remains equal to the original content with only the lifecycle value changed.
- The selection report uses task_planning_pending after the persistence succeeds, while the transition output and empty stderr are preserved.
- Existing tests continue to pass and no forbidden paths are changed.

## Quality Gates to Run
```bash
npx vitest run tests/main.test.ts -t "formalized feature transitions to task_planning_pending"
npx vitest run tests/main.test.ts
npm run typecheck
npm test
git diff --check
```
