# Task 005.1: Remove forbidden state-doc changes from the F002-T05 submission

## Task ID
`F002-T05-C01`

## Parent Task
`F002-T05`

## Parent Feature
`002-configuration-model`

## Goal
Resubmit F002-T05 with a clean patch that excludes the `docs/compassrose/PROJECT_STATE.md` and `docs/features/002-configuration-model/state.md` edits, leaving only the intended runtime-preflight test coverage in scope.

## First Executable Step
Revert or otherwise exclude the changes to `docs/compassrose/PROJECT_STATE.md` and `docs/features/002-configuration-model/state.md` from the F002-T05 patch, then confirm the remaining diff is limited to the intended task work.

## Minimum Progress Evidence
- The resubmitted diff no longer touches `docs/compassrose/PROJECT_STATE.md` or `docs/features/002-configuration-model/state.md`.
- The remaining patch still contains the runtime-preflight coverage in `tests/main.test.ts`.
- `npm run typecheck` and `npm test` both pass on the cleaned submission.

## Review Findings
- The submitted patch changed `docs/` files even though `docs/` is forbidden for F002-T05.
- The review artifact metadata did not match the actual diff because the `docs/` changes were omitted from `implementation.changed_files`.

## Scope
Allowed:
- `docs/compassrose/PROJECT_STATE.md`
- `docs/features/002-configuration-model/state.md`

Forbidden:
- `src/`
- `docs/compassrose/CONFIG.md`
- `src/contracts/`
- `tests/ (other than preserving the existing `tests/main.test.ts` change)`

## Constraints
- Do not add new runtime behavior or broaden the task beyond cleaning the submitted patch.
- Preserve the existing runtime-preflight coverage already added for `tests/main.test.ts`.
- The corrected submission must not contain any net `docs/` changes for F002-T05 review.

## Acceptance Criteria
- The resubmitted diff contains no net changes under `docs/compassrose/PROJECT_STATE.md` or `docs/features/002-configuration-model/state.md`.
- The reviewable task diff is limited to the intended runtime-preflight test coverage.
- The resubmitted artifacts accurately report the files present in the diff.
- `npm run typecheck` and `npm test` pass before re-review.

## Quality Gates to Run
```bash
npm run typecheck
npm test
```
