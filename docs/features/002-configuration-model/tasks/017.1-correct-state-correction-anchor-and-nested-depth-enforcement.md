# Task 017.1: Correct state-correction anchor and nested-depth enforcement

## Task ID
`F002-T17-C1`

## Parent Task
`F002-T17`

## Parent Feature
`002-configuration-model`

## Goal
Enforce the existing limits.max_review_iterations policy against the restored active task and nested correction depth, refusing writes at the boundary and returning the existing non-continuing terminal step result.

## First Executable Step
Remove the attempt-only limitStateCorrectionTaskId helper from src/task/taskId.ts, then run npx vitest run tests/stateCorrectionLimit.test.ts.

## Minimum Progress Evidence
- tests/stateCorrectionLimit.test.ts contains executable coverage for base C1/C2/C3 boundaries, a nested anchor such as F002-T7-C1 at the canonical limit, artifact/state non-creation, and the exitCode 2/continueLoop false result.
- The correction guard uses the restored active task before writeStateCorrectionTask, artifacts.writeJson, and state update calls.
- Compared against ref `023507f3`, the final reviewable diff shows zero net difference in `src/task/taskId.ts` and the original task document -- reached by editing `src/task/taskId.ts` once to delete the leaked helper, not by leaving the file untouched.
- All before-review quality gates pass.

## Review Findings
- Use restoredActiveTask rather than owner.id when checking the bounded correction candidate.
- Count nested -C suffix depth as correction iterations; the current terminal-number-only check allows nested allocation beyond the configured depth.
- Add integration coverage for correct_state, artifact/state non-creation, and the terminal step result.
- Remove the attempt-only task-document scope note and the net taskId.ts helper change so the final diff satisfies the original changed-file acceptance criterion.

## Scope
Allowed:
- `src/orchestrator/orchestrator.ts`
- `src/orchestrator/runtimeHelpers.ts`
- `tests/stateCorrectionLimit.test.ts`
- `src/task/taskId.ts`
- `docs/features/002-configuration-model/tasks/017-add-a-cycle-depth-limit-to-the-correction-task-id-allocator.md`

Forbidden:
- `docs/compassrose/CONFIG.md`
- `docs/compassrose/PROJECT_STATE.md`
- `docs/features/002-configuration-model/feature.md`
- `docs/features/002-configuration-model/architecture.md`
- `docs/features/002-configuration-model/state.md`
- `proto/`
- `src/cli/`
- `all other paths not listed under allowed_paths`

## Constraints
- Use the existing limits.max_review_iterations field and canonical fallback of 1; add no configuration field, manifest, artifact type, or enum.
- Use the same restoredActiveTask anchor that buildStateCorrectionTask receives.
- Refuse before correction markdown, task JSON, feature-state, or project-state writes and return the existing non-continuing non-success result with a diagnostic.
- Keep the focused pure helper in the existing orchestrator/runtimeHelpers.ts boundary if a helper is needed; do not retain a net taskId.ts source change.
- Restore the original task document content; do not rewrite its requirements or add scope notes.
- Do not modify unrelated orchestration, configuration, documentation, proto, or CLI code.

## Acceptance Criteria
- The state-correction guard bounds the candidate for restoredActiveTask, including both same-anchor numbering and nested suffix depth.
- With limit 2, C1 and C2 remain deterministic and C3 is refused before any artifact or state mutation.
- With canonical limit 1, the first correction is allowed, while the next same-anchor or nested correction is refused before writes.
- The correct_state step returns exitCode 2 and continueLoop false with a diagnostic when the limit is reached.
- Tests verify artifact markdown/JSON and feature/project state are unchanged on refusal.
- Compared against ref `023507f3`, the final reviewable diff shows zero net difference in `src/task/taskId.ts` and the task document (achieved by editing `src/task/taskId.ts` to delete the leaked helper, not by avoiding the file), and shows real changes only in `src/orchestrator/orchestrator.ts`, `src/orchestrator/runtimeHelpers.ts`, and `tests/`.
- The focused test, typecheck, full test suite, and diff check pass.

## Quality Gates to Run
```bash
npx vitest run tests/stateCorrectionLimit.test.ts
npm run typecheck
npm test
git diff --check
git diff --name-only --exit-code 023507f3 -- docs/features/002-configuration-model/tasks/017-add-a-cycle-depth-limit-to-the-correction-task-id-allocator.md
git diff --name-only --exit-code 023507f3 -- src/task/taskId.ts
```

Note: the two `--exit-code` gates above were originally written to diff against the
working tree's current `HEAD`, but by the time this correction runs, `HEAD` (commit
`b9643010`) already contains the out-of-scope `taskId.ts` helper and task-document note
that this task's "cleanup" scope exists to remove. A bare `git diff` against `HEAD` can
only pass by leaving that already-committed content untouched -- the opposite of
"restore"/"remove". `023507f3` is the commit immediately before task F002-T17's own
implementation began, so diffing against it actually verifies restoration.

Note: "no net changes to `src/task/taskId.ts`" and "zero net difference" mean the file's
*final content* matches ref `023507f3` exactly -- not that the file must go untouched.
Reaching that state requires one edit: deleting the leaked `limitStateCorrectionTaskId`
helper this task's cleanup exists to remove. `src/task/taskId.ts` is listed under this
task's `allowed_paths` specifically so that edit can happen; do not remove it from scope
or treat it as forbidden. A prior recovery-lesson suggestion to exclude
`src/task/taskId.ts` from scope was written against an earlier, since-superseded version
of this task (when a helper had leaked in as *new*, unreviewed code) and does not apply
to the current cleanup-only requirement -- recovery lessons are advisory hypotheses to
check against this document, never a substitute for it.
