# Task 014.1: Reject missing planners and preserve configured planner invocation

## Task ID
`F002-T14-C1`

## Parent Task
`F002-T14`

## Parent Feature
`002-configuration-model`

## Goal
Correct the existing task_planning_pending branch so an empty repository-configured planner command fails with a task-planning diagnostic, and a non-empty generic external_cli command executes once with its configured args; strengthen the focused test to observe exactly one invocation.

## First Executable Step
In tests/main.test.ts, extend the existing task_planning_pending tests with an empty adapters.external_cli.command case that calls main through its existing stdout/stderr callbacks and asserts a non-zero result plus unchanged state, and make the configured planner test record its invocation count in the existing temporary workspace.

## Minimum Progress Evidence
- The new empty-command test fails against the current implementation because it returns 0 and emits the dispatch message.
- The configured-planner test observes exactly one execution of the temporary command and passes after the source correction.
- The focused task_planning_pending test and all listed quality gates pass after the correction.

## Review Findings
- src/cli/main.ts:198-201,268-279 treats a missing or empty configured planner command as success and emits the dispatch message.
- src/cli/main.ts:201-265 rewrites configured commands by extension, hard-codes node/python/cmd, and drops configured args for shell-script commands.
- tests/main.test.ts:621-675 does not observe invocation count despite claiming exactly-once behavior.

## Scope
Allowed:
- `src/cli/main.ts`
- `tests/main.test.ts`

Forbidden:
- `docs/`
- `src/contracts/`
- `src/config/`
- `src/doctor/`
- `src/proto/`
- `tests/configReader.test.ts`
- `tests/doctorCommand.test.ts`

## Constraints
- Use the existing task_planning_pending selection and repository-local configuration from configResult.value.roles.planner and configResult.value.adapters.
- Execute the configured generic external_cli command and its configured args once; do not add extension-based hard-coded node, python, or cmd substitutions, and do not drop args.
- Keep the existing runtime preflight, deterministic feature ordering, formalized-to-task_planning_pending transition handoff, selected-feature output, and state-content preservation unchanged.
- On an empty or missing planner command, return non-zero with a task-planning diagnostic and do not emit the successful dispatch report or mutate the already task_planning_pending lifecycle state.
- Do not modify global external-tool configuration.

## Acceptance Criteria
- A task_planning_pending feature with a configured command records exactly one planner invocation and retains the selected feature in the deterministic output.
- A missing or empty configured planner command returns non-zero, emits a task-planning diagnostic through the existing stderr path, does not emit the successful dispatch report, and leaves the task_planning_pending state content unchanged.
- The invocation uses the configured command and args directly from the repository-local configuration without hard-coded provider or interpreter selection.
- The existing formalized transition test still passes with the exact task_planning_pending selection line and exact non-lifecycle state content.
- The focused task_planning_pending test, npm run typecheck, npm test, and git diff --check pass.

## Quality Gates to Run
```bash
npx vitest run tests/main.test.ts -t "task_planning_pending"
npm run typecheck
npm test
git diff --check
```
