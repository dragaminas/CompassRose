# Task 016.1: Constrain MVP loader normalization to the configuration boundary

## Task ID
`F002-T16-C1`

## Parent Task
`F002-T16`

## Parent Feature
`002-configuration-model`

## Goal
Remove the F002-T16 CLI-scope leak and correct loader normalization so omitted MVP policy and adapter fields remain acceptable without losing existing canonical configuration data or silently accepting malformed present values.

## First Executable Step
Revert only the F002-T16 git_policy fallback hunk in src/cli/main.ts, run npm run typecheck, and keep any caller-compatibility fix inside src/config/configReader.ts and src/config/configTypes.ts.

## Minimum Progress Evidence
- The reviewable diff contains no F002-T16 behavior change in src/cli/main.ts; any edit there is limited to removing the added fallback hunk.
- tests/configReader.test.ts contains executable coverage showing omitted MVP policy and adapter fields are accepted while the returned result remains compatible with existing typed callers.
- tests/configReader.test.ts contains executable coverage showing malformed present optional adapter fields and non-object optional policy sections return diagnostics rather than being silently coerced or throwing.
- An executable canonical-configuration assertion confirms the existing top-level platform value remains exposed.
- npx vitest run tests/configReader.test.ts tests/doctorCommand.test.ts, npm run typecheck, npm test, and git diff --check all pass.

## Review Findings
- Remove the forbidden src/cli/main.ts fallback hunk; preserve the existing caller-facing typed result through the configuration loader/types only.
- Validate optional external_cli fields conditionally when present; default only omitted fields so invalid present values do not become falsely typed values.
- Guard present optional policy sections as objects before validation and return the existing configuration diagnostics mechanism for malformed sections.
- Restore spreading/preserving the parsed canonical top-level configuration, including platform.

## Scope
Allowed:
- `src/cli/main.ts (remove only the F002-T16 fallback hunk)`
- `src/config/configReader.ts`
- `src/config/configTypes.ts`
- `tests/configReader.test.ts`

Forbidden:
- `src/doctor/`
- `src/orchestrator/`
- `tests/doctorCommand.test.ts`
- `docs/features/002-configuration-model/state.md`
- `docs/compassrose/PROJECT_STATE.md`
- `docs/compassrose/CONFIG.md`
- `provider-specific adapters`
- `external-tool global configuration`
- `planner invocation, feature selection, lifecycle transitions, or other orchestration behavior`

## Constraints
- Keep the documented MVP required fields and missing-versus-empty command semantics unchanged.
- Omitted future-facing policy sections and omitted non-required external_cli fields must remain valid for Doctor.
- Present optional sections and adapter fields must retain deterministic validation and typed behavior when supplied.
- Do not add CLI, Doctor, orchestration, provider-specific, documentation, or global-tool behavior beyond removing the identified forbidden hunk.

## Acceptance Criteria
- src/cli/main.ts has no remaining F002-T16 behavior change.
- A minimal MVP configuration is accepted by readProjectConfiguration() and remains compatible with existing typed callers without a CLI fallback change.
- Malformed present optional adapter fields and non-object optional policy sections produce configuration diagnostics rather than silent defaults or exceptions.
- The complete canonical configuration preserves its existing policy, adapter, and top-level platform values.
- The focused configuration and Doctor tests, typecheck, full test suite, and diff whitespace check pass.

## Quality Gates to Run
```bash
npx vitest run tests/configReader.test.ts tests/doctorCommand.test.ts
npm run typecheck
npm test
git diff --check
```
